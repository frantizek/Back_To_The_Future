/**
 * EjemploFinal
 * 
 * Todos los metodos y variables de instanca se pueden sobreescribir por defecto, 
 * en caso de que se deseara lo contrario se debera aplicar el modificador final, 
 * lo cual implica que todas las referencias futuras a este elemento se basaran 
 * en esta definicio, son equivalentes a la definicion de una constante en C++.
 * 
 * Se recomienda que los identificadores sean en mayusculas, en el caso de 
 * declarar los metodos como final mejora el rendimiento del compilador ya que
 * puede realizar llamadas en linea a dichos metodos.
 */
class EjemploFinal {
    
    final int MAX_LINEAS = 10;
    final int MAX_COLUMNAS = 80;
    
}
